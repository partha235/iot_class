# access point

try:
  import usocket as socket
except:
  import socket

import network

import esp
esp.osdebug(None)

import gc
gc.collect()

ssid = 'bps'
password = 'bps12345'

ap = network.WLAN(network.AP_IF)
ap.active(True)
ap.config(essid=ssid, password=password)

while ap.active() == False:
  pass

print('Connection successful')
print(ap.ifconfig())

def web_page():
  html = """<html>
    <head><title>IoT class</title><style>body{background-color: rgb(19, 24, 24);}</style>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta http-equiv="refresh"content="0.1"></head>
    <body>
        <h1 style="color:coral">BPS EXPLORE</h1>
        <p style="color:azure">This is a test program made for IoT class of program file test5.mpy 
            where wifi is made in <b>access point mode</b>. </p>
    </body>
    </html>"""
  return html

s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
s.bind(('', 80))
s.listen(5)

while True:
  try:
    x.toggle()
    if gc.mem_free() < 102000:
      gc.collect()
    conn, addr = so.accept()
    conn.settimeout(3.0)
    print('Got a connection from %s' % str(addr))
    request = conn.recv(1024)
    conn.settimeout(None)
    request = str(request)
    print('Content = %s' % request)
    response = web_page()
    conn.send('HTTP/1.1 200 OK\n')
    conn.send('Content-Type: text/html\n')
    conn.send('Connection: close\n\n')
    conn.sendall(response)
    conn.close()
    sleep_ms(100)
  except OSError as e:
    conn.close()
    print('Connection closed')